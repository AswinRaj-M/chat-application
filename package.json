{
    "name": "chat-application-root",
    "version": "1.0.0",
    "scripts": {
        "build": "cd server && npm install",
        "start": "cd server && node seed.js && node index.js"
    },
    "engines": {
        "node": ">=14.x"
    }
}